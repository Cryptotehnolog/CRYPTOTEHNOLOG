[portfolio]
starting_capital_usd = 100000.0
max_position_notional_fraction = 0.01
max_strategy_notional_fraction = 0.05
max_total_margin_fraction = 0.25
daily_loss_limit_fraction = 0.02
max_drawdown_fraction = 0.05

[data_quality]
reject_stale_market_data = true
max_clock_skew_ms = 1000

